# AgentGuard 试用反馈表

> 填写预计 5–10 分钟。请复制本文件后填写，并命名为
> `AgentGuard_Pilot_Feedback_你的编号.md`。

安全提醒：不要粘贴完整配置、完整报告、API Key、Token、私钥或未脱敏的内部域名。风险请优先
使用规则 ID 描述；必须提及端点时请用 `example.com` 等内容替代。

## 1. 基本信息

- 试用编号：
- 日期：
- 试用入口：CLI / macOS Desktop（非 CLI）
- 用户类型：个人重度用户 / 团队负责人 / 安全或平台人员 / 其他：
- 操作系统：
- Node.js 版本（仅 CLI cohort）：
- 日常使用的 Agent：
- 是否使用自定义 Provider、中转 API 或 CC Switch：是 / 否
- 是否使用 MCP：是 / 否

## 2. 安装与首次使用

- 是否安装成功：是 / 否
- 安装用时：约 ____ 分钟
- 完成首次有效扫描总用时：约 ____ 分钟
- 是否能只看说明独立完成：是 / 否
- 如果失败或需要帮助，请简述卡在哪一步：

CLI cohort：

| 命令 | 是否成功 | 输出是否看懂 | 价值评分（1–5） | 备注 |
|---|---|---|---:|---|
| `agentguard doctor` | 是 / 否 | 是 / 部分 / 否 |  |  |
| `agentguard scan` | 是 / 否 | 是 / 部分 / 否 |  |  |
| `agentguard map` | 是 / 否 | 是 / 部分 / 否 |  |  |
| `agentguard report --format html` | 是 / 否 | 是 / 部分 / 否 |  |  |
| `baseline --dry-run`（如执行） | 是 / 否 / 未执行 | 是 / 部分 / 否 |  |  |
| 报告中的本机修复命令（如执行） | 是 / 否 / 未执行 | 是 / 部分 / 否 |  |  |
| `agentguard risk accept/list/revoke`（如执行） | 是 / 否 / 未执行 | 是 / 部分 / 否 |  |  |
| `agentguard trust add/list/remove`（如执行） | 是 / 否 / 未执行 | 是 / 部分 / 否 |  |  |
| `agentguard ignore add/list/remove`（如执行） | 是 / 否 / 未执行 | 是 / 部分 / 否 |  |  |

macOS Desktop 非 CLI cohort：

- 是否从受邀 DMG 拖入 Applications 并从 Applications 启动：是 / 否 / 未试用桌面版
- 是否出现“仍要打开”、应用损坏或其它 Gatekeeper 绕过提示：是 / 否 / 未试用桌面版
- 是否理解应选择单个代码项目根目录：是 / 部分 / 否 / 未试用桌面版
- 是否在没有帮助的情况下完成项目选择和首次扫描：是 / 否 / 未试用桌面版
- 项目扫描是否请求了与所选项目无关的“桌面”“文稿”“下载”等权限：是 / 否 / 未试用桌面版
- 是否能在 3 分钟内指出最优先的 Agent 和任务：是 / 否 / 未试用桌面版
- 是否完成任务详情查看和一次复扫验证：是 / 否 / 未执行
- HTML 报告是否成功导出并重新打开：是 / 否 / 未执行
- 是否能从顶部“报告与支持”找到“导出脱敏诊断”：是 / 否 / 未试用桌面版
- 诊断 JSON 是否成功导出：是 / 否 / 未执行
- 导出前是否明确知道文件不会自动上传：是 / 否
- 是否愿意在确认内容后把诊断 JSON 与本反馈表一并提供：是 / 否 / 未执行

## 3. 扫描结果

- 有效发现数量：____
- 误报或不认可数量：____
- 已完成整改数量：____
- 计划整改数量：____
- 已接受风险任务数量：____
- 项目级忽略规则数量：____
- 复扫后确认已解决数量：____

请只填写规则 ID 和简短结论，不要粘贴完整报告。

| 规则 ID | 此前是否知道 | 是否有用 | 采取的行动或计划 |
|---|---|---|---|
|  | 是 / 否 | 是 / 否 |  |
|  | 是 / 否 | 是 / 否 |  |
|  | 是 / 否 | 是 / 否 |  |

误报或不认可项：

| 规则 ID | 误报或不认可的原因 |
|---|---|
|  |  |
|  |  |

## 4. 首个行动任务闭环

如果你尝试处理或接受了一个任务，请只填写 task ID，不要粘贴完整配置或端点：

| 字段 | 填写内容 |
|---|---|
| task ID |  |
| 是否在 60 秒内理解为什么要处理 | 是 / 部分 / 否 |
| 是否复制了报告命令 | 是 / 否 / 不适用 |
| 命令是否匹配操作系统和 Shell | 是 / 部分 / 否 / 不适用 |
| 是否理解命令是完整修复还是辅助步骤 | 是 / 否 / 不确定 |
| `risk verify` 输出 | 已解决 / 已缓解 / 仍存在 / 已接受 / 身份变化 / 未执行 |
| 端点信任（如适用） | 未执行 / 已登记 / 已撤销 / 不适用 |
| 项目规则忽略（如适用） | 未执行 / 已忽略 / 已撤销 / 不适用 |
| 最终结果 | 已解决 / 已接受 / 已按项目忽略 / 仍存在 / 放弃 |
| 完成用时 | ____ 分钟 |
| 卡住阶段 | 理解 / 执行 / 修改配置 / 认证验证 / 轮换旧凭证 / 复扫 |

- 执行后是否知道需要重新扫描并生成新报告：是 / 否
- `risk verify` 后能否明确判断目标任务是否解决：是 / 部分 / 否 / 未使用
- 聚合任务中的关联规则是否容易理解：是 / 部分 / 否
- 如果使用风险接受，是否理解它会影响默认待办和退出码：是 / 否 / 未使用
- 是否能区分“接受当前任务”“信任 Provider 端点”“按项目忽略规则”：是 / 部分 / 否 / 未使用
- 如果使用项目规则忽略，是否理解它会跨 evidence/task ID 变化继续生效：是 / 否 / 未使用
- 撤销项目规则忽略后，相关发现是否重新出现：是 / 否 / 未验证
- 是否发现一个项目的接受状态影响了另一个项目：是 / 否 / 未验证
- Keychain / Secret Service / DPAPI 指引是否符合你的系统：是 / 部分 / 否 / 未使用
- 是否理解存入安全存储后仍可能需要配置 helper/launcher 并轮换旧凭证：是 / 否 / 未使用

## 5. 总体评价

- 最有价值的能力：首次扫描 / Agent 工作区 / 连接链路 / report / 修复步骤 / baseline / 风险接受 / 端点信任 / 项目规则忽略 / risk verify / CI
- 你是否愿意再次使用：是 / 否 / 不确定
- 你是否愿意在团队中试用：是 / 否 / 不确定
- 推荐给另一位多 Agent 用户的可能性（0–10）：____
- 什么情况下你会再次运行 AgentGuard？

- 本次最大的阻碍是什么？

- 如果只能改进一件事，你希望改什么？

- 请用一句话评价这次试用：

## 6. 安全与可靠性确认

- 是否看到 AgentGuard 输出完整密钥或其他不应暴露的信息：是 / 否
- 是否发生配置损坏：是 / 否 / 未执行自动整改
- 是否发生备份或恢复失败：是 / 否 / 未执行
- 桌面 baseline 是否先看到了 diff 和原生确认框：是 / 否 / 未执行桌面整改
- 桌面应用后是否看到了复扫结果和恢复入口：是 / 否 / 未执行桌面整改
- 是否错误隐藏了仍应处理的其它项目、Agent 或关联规则：是 / 否 / 未使用接受/忽略
- 是否看到把密钥写入 shell profile、普通 `.env` 或 Windows 用户环境的建议：是 / 否
- 脱敏诊断中是否出现项目路径、Provider 端点、task ID、配置内容或原始错误文本：是 / 否 / 未执行
- 如果以上任一项为“是”，请只描述现象，不要附带敏感内容：
