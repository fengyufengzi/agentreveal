function action(input) {
    const { family, evidenceKeys = [], ...rest } = input;
    return { ...rest, group: { family, evidenceKeys } };
}
const verifyRescan = ["完成处置后重新运行 agentguard scan，确认该 finding 已消失或符合接受条件。"];
const verifyMcp = ["核对 MCP 的真实来源、权限和启动方式后重新扫描，并确认没有意外配置。"];
const verifyParse = ["修复配置格式或读取条件后重新扫描，确认该 Agent 的发现数量和配置内容恢复正常。"];
/**
 * 每条具体规则的下一步行动。
 *
 * severity 描述潜在影响；这里的 priority 描述用户现在是否需要行动。两者故意分离。
 * `satisfies Record<RuleId, FindingAction>` 保证 RuleId 新增时必须同步补充处置定义。
 */
export const ACTION_MATRIX = {
    CLAUDE_LOCAL_BASE_URL: action({
        disposition: "observe", priority: "P3", confidence: "high", fixMode: "none",
        rationale: "本地地址只是请求第一跳，本身不是漏洞；真实风险取决于监听进程和代理上游。",
        nextSteps: ["确认本地端口对应的进程、所有者和真实上游。"],
        verification: ["将本地端点与代理/CC Switch 结果对照，确认最终上游符合预期。"],
        acceptWhen: "本地进程受控且所有真实上游均已审核可信。",
        family: "provider.proxy-chain", evidenceKeys: ["baseUrl"],
    }),
    CLAUDE_UNKNOWN_BASE_URL: action({
        disposition: "review", priority: "P1", confidence: "medium", fixMode: "manual",
        rationale: "Claude 请求和源码上下文会发送到未知或疑似中转端点。",
        nextSteps: ["核对端点所有者、TLS、数据保留政策；不可信则切回官方端点。"],
        verification: verifyRescan,
        acceptWhen: "端点是经批准的内部/自建 HTTPS 服务，并已登记为 trusted/internal。",
        family: "provider.endpoint", evidenceKeys: ["baseUrl"],
    }),
    CLAUDE_INSECURE_HTTP: action({
        disposition: "fix", priority: "P1", confidence: "high", fixMode: "guided",
        rationale: "非本地明文 HTTP 会暴露凭证、提示词和源码上下文。",
        nextSteps: ["把 ANTHROPIC_BASE_URL 改为证书有效的 HTTPS 端点。"],
        verification: verifyRescan,
        acceptWhen: "仅隔离网络且不传输敏感数据时可限时接受。",
        family: "provider.endpoint", evidenceKeys: ["baseUrl"],
    }),
    CLAUDE_PLAINTEXT_TOKEN: action({
        disposition: "fix", priority: "P0", confidence: "high", fixMode: "guided",
        rationale: "settings 文件中的真实凭证泄露会直接导致有效凭证泄露；已知代理接管占位符不属于此规则。",
        nextSteps: ["先用 Desktop 一键备份或运行 agentguard credential backup <task-id>，再把新凭证存入系统安全存储，从 settings.json/settings.local.json 删除明文字段，改用可信 apiKeyHelper，并轮换旧凭证。"],
        verification: verifyRescan,
        acceptWhen: "仅短期、最小权限、短有效期凭证可作为限时例外。",
        family: "secret.plaintext", evidenceKeys: [],
    }),
    CLAUDE_API_KEY_HELPER: action({
        disposition: "review", priority: "P2", confidence: "high", fixMode: "manual",
        rationale: "apiKeyHelper 会执行外部命令，安全性取决于脚本来源和写权限。",
        nextSteps: ["核对 helper 的绝对路径、所有者、权限、内容和依赖来源。"],
        verification: ["确认脚本不可被非授权用户修改，且不会执行不可信输入。"],
        acceptWhen: "helper 来源可信、权限收紧且行为符合预期。",
        family: "secret.helper", evidenceKeys: [],
    }),
    CLAUDE_BYPASS_PERMISSIONS: action({
        disposition: "fix", priority: "P0", confidence: "high", fixMode: "baseline",
        rationale: "bypassPermissions 会默认跳过所有工具审批。",
        nextSteps: ["使用 baseline，或手动把 defaultMode 改为 default/acceptEdits。"],
        verification: verifyRescan,
        acceptWhen: "仅无真实凭证和数据的一次性隔离沙箱可限时接受。",
        baselineProfiles: { safe: "resolve", balanced: "resolve" },
        family: "permission.execution", evidenceKeys: ["defaultMode"],
    }),
    CLAUDE_DANGEROUS_ALLOW: action({
        disposition: "fix", priority: "P1", confidence: "high", fixMode: "baseline",
        rationale: "无约束 Bash 或通配 allow 等价于放行任意命令。",
        nextSteps: ["使用 baseline 移除无约束规则，或手动改为最小命令前缀。"],
        verification: verifyRescan,
        acceptWhen: "只可在一次性隔离环境中限时接受。",
        baselineProfiles: { safe: "resolve", balanced: "resolve" },
        family: "permission.execution", evidenceKeys: ["rules"],
    }),
    CLAUDE_HOOKS_PRESENT: action({
        disposition: "review", priority: "P2", confidence: "high", fixMode: "manual",
        rationale: "hooks 会自动执行 shell，是否危险取决于具体命令及脚本权限。",
        nextSteps: ["逐项审核 hook 事件、命令、脚本来源和写权限。"],
        verification: ["确认所有 hook 都是预期配置且不可被非授权用户篡改。"],
        acceptWhen: "命令及依赖均可信、版本固定、权限最小化。",
        family: "permission.hooks", evidenceKeys: [],
    }),
    CLAUDE_ENABLE_ALL_PROJECT_MCP: action({
        disposition: "fix", priority: "P1", confidence: "high", fixMode: "baseline",
        rationale: "自动启用仓库声明的所有 MCP，会把仓库写权限扩大为本地工具执行权限。",
        nextSteps: ["使用 baseline 关闭自动启用，改为逐个审核和批准项目 MCP。"],
        verification: verifyRescan,
        acceptWhen: "仅受控仓库且全部项目 MCP 已审核时可限时接受。",
        baselineProfiles: { safe: "resolve", balanced: "resolve" },
        family: "mcp.auto-enable", evidenceKeys: [],
    }),
    CLAUDE_MCP_REMOTE: action({
        disposition: "review", priority: "P2", confidence: "medium", fixMode: "manual",
        rationale: "远程 MCP 可读取上下文并影响 Agent，需确认端点归属与权限。",
        nextSteps: ["核对 MCP 所有者、TLS、认证、数据范围和保留政策。"],
        verification: verifyMcp,
        acceptWhen: "经批准的 HTTPS MCP，且数据与工具权限最小化。",
        family: "mcp.server", evidenceKeys: ["server", "scope", "url"],
    }),
    CLAUDE_MCP_STDIO: action({
        disposition: "observe", priority: "P3", confidence: "high", fixMode: "none",
        rationale: "stdio MCP 是正常的能力清单；其存在本身不是漏洞，但仍应确认来源和版本。",
        nextSteps: ["核对可执行文件来源、真实路径、版本锁定、参数和写权限。"],
        verification: verifyMcp,
        acceptWhen: "来源可信、版本固定且程序不可被非授权用户替换。",
        family: "mcp.server", evidenceKeys: ["server", "scope", "command"],
    }),
    CLAUDE_MCP_SECRET_ENV: action({
        disposition: "review", priority: "P1", confidence: "low", fixMode: "guided",
        rationale: "键名疑似凭证，但当前证据不能区分明文值和安全引用。",
        nextSteps: ["人工确认值类型；若为明文则改用安全注入并轮换凭证。"],
        verification: ["确认配置中不再保存明文值；必要时记录为引用型误报。"],
        acceptWhen: "值只是安全引用/占位符，或凭证是经批准的短期例外。",
        family: "mcp.server", evidenceKeys: ["server"],
    }),
    CLAUDE_PARSE_FAILED: action({
        disposition: "review", priority: "P1", confidence: "high", fixMode: "guided",
        rationale: "解析失败会造成扫描覆盖盲区。",
        nextSteps: ["校验 settings.json、settings.local.json 和全局状态文件的 JSON 格式及权限。"],
        verification: verifyParse,
        family: "coverage.parse", evidenceKeys: ["path"],
    }),
    CODEX_CUSTOM_PROVIDER: action({
        disposition: "review", priority: "P1", confidence: "medium", fixMode: "manual",
        rationale: "自定义模型端点可能接收源码上下文，需确认实际归属。",
        nextSteps: ["核对端点所有者、TLS 和数据政策；不可信则切回官方 Provider。"],
        verification: verifyRescan,
        acceptWhen: "经批准的内部/自建 HTTPS 端点，并已登记为 trusted/internal。",
        family: "provider.endpoint", evidenceKeys: ["provider", "baseUrl"],
    }),
    CODEX_INSECURE_HTTP: action({
        disposition: "fix", priority: "P1", confidence: "high", fixMode: "guided",
        rationale: "公网或非受控网络上的明文 HTTP 会暴露凭证和上下文。",
        nextSteps: ["把 model_provider 的 base_url 改为证书有效的 HTTPS。"],
        verification: verifyRescan,
        acceptWhen: "loopback 第一跳，或隔离私网中的限时例外。",
        family: "provider.endpoint", evidenceKeys: ["provider", "baseUrl"],
    }),
    CODEX_PLAINTEXT_API_KEY: action({
        disposition: "fix", priority: "P0", confidence: "high", fixMode: "guided",
        rationale: "auth.json 中的真实 API Key 泄露会直接暴露凭证；已知代理接管占位符不属于此规则。",
        nextSteps: ["删除落盘 Key，改用 OAuth/安全注入，收紧文件权限并轮换旧 Key。"],
        verification: verifyRescan,
        acceptWhen: "仅短期、最小权限、短有效期 Key 可作为限时例外。",
        family: "secret.plaintext", evidenceKeys: ["authMode"],
    }),
    CODEX_MCP_REMOTE: action({
        disposition: "review", priority: "P2", confidence: "medium", fixMode: "manual",
        rationale: "远程 MCP 可读取上下文和注入工具行为。",
        nextSteps: ["核对端点所有者、TLS、认证、权限和数据保留政策。"],
        verification: verifyMcp,
        acceptWhen: "经批准的 HTTPS MCP，且数据与工具权限最小化。",
        family: "mcp.server", evidenceKeys: ["server", "url"],
    }),
    CODEX_MCP_STDIO: action({
        disposition: "observe", priority: "P3", confidence: "high", fixMode: "none",
        rationale: "stdio MCP 是正常的能力清单；其存在本身不是漏洞，但仍应确认来源和版本。",
        nextSteps: ["核对命令真实路径、包来源、版本、参数和写权限。"],
        verification: verifyMcp,
        acceptWhen: "来源可信、版本固定且命令不可被非授权用户替换。",
        family: "mcp.server", evidenceKeys: ["server", "command"],
    }),
    CODEX_MCP_SECRET_ENV: action({
        disposition: "review", priority: "P1", confidence: "low", fixMode: "guided",
        rationale: "键名疑似凭证，但当前证据不能区分明文值和安全引用。",
        nextSteps: ["人工确认值类型；若为明文则改用安全注入并轮换凭证。"],
        verification: ["确认 config.toml 不再保存明文值；引用型配置可记录为误报。"],
        acceptWhen: "值只是安全引用/占位符，或凭证是经批准的短期例外。",
        family: "mcp.server", evidenceKeys: ["server"],
    }),
    CODEX_TRUSTED_PROJECTS: action({
        disposition: "cleanup", priority: "P2", confidence: "high", fixMode: "guided",
        rationale: "trusted 数量不是漏洞，风险来自过期或可被他人写入的免审批路径。",
        nextSteps: ["逐项删除不再使用、已不存在或写权限不受控的 trusted 项目。"],
        verification: verifyRescan,
        acceptWhen: "项目仍在使用、归属可信且仓库写权限受控。",
        family: "permission.trusted-projects", evidenceKeys: ["projects"],
    }),
    CODEX_LOCAL_PROXY: action({
        disposition: "observe", priority: "P3", confidence: "medium", fixMode: "none",
        rationale: "代理是请求链路事实，本身不是漏洞；真实风险取决于代理和上游。",
        nextSteps: ["确认代理地址、监听进程、规则和真实上游。"],
        verification: ["将代理结果与 Provider/CC Switch 结果对照，确认链路符合预期。"],
        acceptWhen: "代理是预期配置，进程和所有上游均可信。",
        family: "provider.proxy-chain", evidenceKeys: ["proxyUrl"],
    }),
    CODEX_PARSE_FAILED: action({
        disposition: "review", priority: "P1", confidence: "high", fixMode: "guided",
        rationale: "解析失败会造成 Codex 配置扫描盲区。",
        nextSteps: ["校验 config.toml 格式和读取权限。"],
        verification: verifyParse,
        family: "coverage.parse", evidenceKeys: ["path"],
    }),
    CCSWITCH_SCHEMA_UNKNOWN: action({
        disposition: "review", priority: "P2", confidence: "high", fixMode: "manual",
        rationale: "数据库可读取但 schema 未验证，扫描结果可能不完整。",
        nextSteps: ["记录 schema 版本，升级 AgentGuard，必要时人工抽查 Provider 和代理数量。"],
        verification: ["使用支持该 schema 的版本复扫，并确认解析结果完整。"],
        acceptWhen: "已人工确认关键字段仍被完整解析。",
        family: "coverage.schema", evidenceKeys: ["schemaVersion"],
    }),
    CCSWITCH_UNKNOWN_BASE_URL: action({
        disposition: "review", priority: "P1", confidence: "medium", fixMode: "manual",
        rationale: "未知 Provider 可能接收源码上下文，也可能是合法内部服务。",
        nextSteps: ["在 CC Switch 中核对端点归属；删除、切换或登记为可信。"],
        verification: verifyRescan,
        acceptWhen: "经批准的内部/自建 HTTPS 端点，并已登记为 trusted/internal。",
        family: "provider.endpoint", evidenceKeys: ["appType", "provider", "baseUrl"],
    }),
    CCSWITCH_RELAY_ENDPOINT: action({
        disposition: "review", priority: "P1", confidence: "medium", fixMode: "manual",
        rationale: "疑似中转端点会集中接收源码和凭证，需要确认运营者。",
        nextSteps: ["核对端点运营者、TLS 和数据政策；不可信则切回官方端点。"],
        verification: verifyRescan,
        acceptWhen: "组织自建且受控、使用 HTTPS、数据处理政策明确。",
        family: "provider.endpoint", evidenceKeys: ["appType", "provider", "baseUrl"],
    }),
    CCSWITCH_INSECURE_HTTP: action({
        disposition: "fix", priority: "P1", confidence: "high", fixMode: "manual",
        rationale: "明文 HTTP 可暴露凭证和上下文；本地第一跳需结合代理链路判断。",
        nextSteps: ["在 CC Switch 中改用 HTTPS，或确认该地址只是受控 loopback 第一跳。"],
        verification: verifyRescan,
        acceptWhen: "loopback 第一跳，或隔离私网中的限时例外。",
        family: "provider.endpoint", evidenceKeys: ["appType", "provider", "baseUrl"],
    }),
    CCSWITCH_PLAINTEXT_KEY: action({
        disposition: "fix", priority: "P0", confidence: "medium", fixMode: "manual",
        rationale: "数据库中的非空疑似密钥字段可能直接暴露有效凭证；已知代理接管占位符除外。",
        nextSteps: [
            "先在 Provider 控制台创建独立、最小权限的新 Token，在 CC Switch 原应用中替换并测试，再撤销旧 Token。",
            "普通 Provider 的 API Key/Token 输入框当前不解析环境变量名、${VAR} 或 {env:VAR}；不要把变量名当作 Token 填入。",
            "执行报告中的 Terminal 命令，收紧 ~/.cc-switch、数据库和数据库备份的本机访问权限。",
            "若该 Provider 支持官方账号/OAuth，优先改用不在 CC Switch 数据库保存 Token 的登录方式。",
        ],
        verification: [
            "重新激活 Provider 并测试鉴权，再点击“复扫验证”；只要 CC Switch 数据库仍保存真实 Token，本规则会保留，不能把权限加固误报为已彻底解决。",
        ],
        acceptWhen: "字段并非凭证时可记为误报；真实明文只可作为短期限时例外。",
        family: "secret.plaintext", evidenceKeys: ["providers"],
    }),
    CCSWITCH_SHARED_KEY: action({
        disposition: "fix", priority: "P1", confidence: "high", fixMode: "manual",
        rationale: "多个用途复用同一密钥会扩大单次泄露的影响面。",
        nextSteps: ["为不同 Agent/用途分配独立密钥，并轮换全部旧副本。"],
        verification: verifyRescan,
        acceptWhen: "仅完全相同的服务、权限、用途和轮换周期可限时接受。",
        family: "secret.key-reuse", evidenceKeys: ["fingerprint"],
    }),
    CCSWITCH_PROXY_ENABLED: action({
        disposition: "observe", priority: "P3", confidence: "high", fixMode: "none",
        rationale: "CC Switch 代理服务和该 Agent 路由均开启时才形成接管链路，本身不算待修复漏洞。",
        nextSteps: ["确认接管的 Agent、监听进程、当前上游和自动故障转移队列。"],
        verification: ["将该代理与各 Agent 的本地端点结果关联，确认链路符合预期。"],
        acceptWhen: "代理、真实上游和 failover 队列均可信。",
        family: "provider.proxy-chain", evidenceKeys: ["appType", "proxy", "proxyOwner", "authMode"],
    }),
    CCSWITCH_PROXY_FAILOVER_UNKNOWN: action({
        disposition: "fix", priority: "P1", confidence: "medium", fixMode: "manual",
        rationale: "自动故障转移可能在无感情况下把请求发送到未知上游。",
        nextSteps: ["从 failover 队列移除未知端点，或在审核后明确登记为可信。"],
        verification: verifyRescan,
        acceptWhen: "队列内每个端点均已审核并登记为可信。",
        family: "provider.failover", evidenceKeys: ["appType"],
    }),
    CCSWITCH_PARSE_FAILED: action({
        disposition: "review", priority: "P1", confidence: "high", fixMode: "manual",
        rationale: "数据库解析失败会使 CC Switch 的 Provider、密钥和代理风险全部不可见。",
        nextSteps: ["检查数据库完整性、读取权限、文件锁和 schema 兼容性。"],
        verification: verifyParse,
        family: "coverage.parse", evidenceKeys: ["path"],
    }),
    OPENCODE_CUSTOM_PROVIDER: action({
        disposition: "review", priority: "P2", confidence: "medium", fixMode: "manual",
        rationale: "自定义模型端点会接收源码上下文，需确认实际归属。",
        nextSteps: ["核对端点所有者、TLS 和数据政策；不可信则改回官方端点。"],
        verification: verifyRescan,
        acceptWhen: "经批准的内部/自建 HTTPS 端点，并已登记为 trusted/internal。",
        family: "provider.endpoint", evidenceKeys: ["provider", "baseUrl"],
    }),
    OPENCODE_INSECURE_HTTP: action({
        disposition: "fix", priority: "P1", confidence: "high", fixMode: "guided",
        rationale: "非本地明文 HTTP 会暴露凭证和源码上下文。",
        nextSteps: ["把 provider baseURL 改为证书有效的 HTTPS。"],
        verification: verifyRescan,
        acceptWhen: "仅隔离网络且不传输敏感数据时可限时接受。",
        family: "provider.endpoint", evidenceKeys: ["provider", "baseUrl"],
    }),
    OPENCODE_PLAINTEXT_KEY: action({
        disposition: "fix", priority: "P0", confidence: "high", fixMode: "guided",
        rationale: "apiKey 字面量随配置文件泄露。",
        nextSteps: ["改用 {env:VAR} 引用并轮换已落盘的旧密钥。"],
        verification: verifyRescan,
        acceptWhen: "仅短期、最小权限、短有效期密钥可作为限时例外。",
        family: "secret.plaintext", evidenceKeys: ["provider"],
    }),
    OPENCODE_BASH_UNRESTRICTED: action({
        disposition: "fix", priority: "P0", confidence: "high", fixMode: "baseline",
        rationale: "Bash 无需确认即可执行任意宿主机命令。",
        nextSteps: ["使用 baseline，或手动把 permission.bash 改为 ask/受限规则。"],
        verification: verifyRescan,
        acceptWhen: "仅无真实凭证和数据的一次性隔离沙箱可限时接受。",
        baselineProfiles: { safe: "resolve", balanced: "resolve" },
        family: "permission.execution", evidenceKeys: ["bash"],
    }),
    OPENCODE_PERMISSION_WILDCARD: action({
        disposition: "fix", priority: "P1", confidence: "high", fixMode: "baseline",
        rationale: "整体 allow 会让编辑、网络和命令工具全部免确认。",
        nextSteps: ["使用 baseline 改为显式分项权限；safe 会全面询问，balanced 优先收紧 Bash。"],
        verification: verifyRescan,
        acceptWhen: "仅一次性隔离环境可限时接受。",
        baselineProfiles: { safe: "resolve", balanced: "mitigate" },
        family: "permission.execution", evidenceKeys: ["bash", "edit"],
    }),
    OPENCODE_SHARE_AUTO: action({
        disposition: "fix", priority: "P1", confidence: "high", fixMode: "baseline",
        rationale: "自动分享会在未经逐次确认时上传会话、代码和上下文。",
        nextSteps: ["使用 baseline 将 share 改为 manual 或 disabled。"],
        verification: verifyRescan,
        baselineProfiles: { safe: "resolve", balanced: "resolve" },
        family: "privacy.auto-share", evidenceKeys: ["share"],
    }),
    OPENCODE_AUTOUPDATE_ON: action({
        disposition: "cleanup", priority: "P2", confidence: "high", fixMode: "baseline",
        rationale: "自动更新扩大供应链信任面；受管环境应审阅后升级。",
        nextSteps: ["使用 baseline 关闭自动更新，并纳入受控升级流程。"],
        verification: verifyRescan,
        acceptWhen: "组织已有可信的自动更新、签名验证和回滚机制。",
        baselineProfiles: { safe: "resolve", balanced: "resolve" },
        family: "supply-chain.update", evidenceKeys: [],
    }),
    OPENCODE_MCP_REMOTE: action({
        disposition: "review", priority: "P2", confidence: "medium", fixMode: "manual",
        rationale: "远程 MCP 可读取上下文并影响 Agent。",
        nextSteps: ["核对端点所有者、TLS、认证、权限和数据保留政策。"],
        verification: verifyMcp,
        acceptWhen: "经批准的 HTTPS MCP，且数据与工具权限最小化。",
        family: "mcp.server", evidenceKeys: ["server", "url"],
    }),
    OPENCODE_MCP_LOCAL: action({
        disposition: "observe", priority: "P3", confidence: "high", fixMode: "none",
        rationale: "本地 MCP 是正常的能力清单；其存在本身不是漏洞，但仍应确认来源和版本。",
        nextSteps: ["核对命令真实路径、包来源、版本、参数和写权限。"],
        verification: verifyMcp,
        acceptWhen: "来源可信、版本固定且命令不可被非授权用户替换。",
        family: "mcp.server", evidenceKeys: ["server", "command"],
    }),
    OPENCODE_MCP_SECRET_ENV: action({
        disposition: "review", priority: "P1", confidence: "low", fixMode: "guided",
        rationale: "env/headers 的疑似密钥需要确认是否为明文字面量。",
        nextSteps: ["将明文改为 {env:VAR} 引用并轮换旧凭证。"],
        verification: verifyRescan,
        acceptWhen: "配置已经使用安全引用，或字段不是凭证。",
        family: "mcp.server", evidenceKeys: ["server"],
    }),
    OPENCODE_PARSE_FAILED: action({
        disposition: "review", priority: "P1", confidence: "high", fixMode: "guided",
        rationale: "解析失败会造成 OpenCode 配置扫描盲区。",
        nextSteps: ["校验 opencode.json/JSONC 的格式和读取权限。"],
        verification: verifyParse,
        family: "coverage.parse", evidenceKeys: ["path"],
    }),
    GEMINI_PLAINTEXT_ENV_KEY: action({
        disposition: "fix", priority: "P0", confidence: "high", fixMode: "guided",
        rationale: ".env 中的真实明文凭证可能因文件读取、备份或误提交而泄露；已知代理接管占位符不属于此规则。",
        nextSteps: ["迁移到安全环境注入，收紧文件权限并轮换旧凭证。"],
        verification: verifyRescan,
        acceptWhen: "仅短期、最小权限、短有效期凭证可作为限时例外。",
        family: "secret.plaintext", evidenceKeys: ["keys"],
    }),
    GEMINI_MCP_TRUST_BYPASS: action({
        disposition: "fix", priority: "P0", confidence: "high", fixMode: "baseline",
        rationale: "trust=true 会让该 MCP 的所有工具调用绕过确认。",
        nextSteps: ["使用 baseline 将 trust 改为 false，恢复逐次确认。"],
        verification: verifyRescan,
        acceptWhen: "仅完全隔离且无真实数据的一次性环境可限时接受。",
        baselineProfiles: { safe: "resolve", balanced: "resolve" },
        family: "mcp.server", evidenceKeys: ["server"],
    }),
    GEMINI_MCP_REMOTE: action({
        disposition: "review", priority: "P2", confidence: "medium", fixMode: "manual",
        rationale: "远程 MCP 可读取上下文并影响 Agent。",
        nextSteps: ["核对端点所有者、TLS、认证、权限和数据保留政策。"],
        verification: verifyMcp,
        acceptWhen: "经批准的 HTTPS MCP，且数据与工具权限最小化。",
        family: "mcp.server", evidenceKeys: ["server", "url"],
    }),
    GEMINI_MCP_STDIO: action({
        disposition: "observe", priority: "P3", confidence: "high", fixMode: "none",
        rationale: "stdio MCP 是正常的能力清单；其存在本身不是漏洞，但仍应确认来源和版本。",
        nextSteps: ["核对命令真实路径、包来源、版本、参数和写权限。"],
        verification: verifyMcp,
        acceptWhen: "来源可信、版本固定且命令不可被非授权用户替换。",
        family: "mcp.server", evidenceKeys: ["server", "command"],
    }),
    GEMINI_MCP_SECRET_ENV: action({
        disposition: "review", priority: "P1", confidence: "low", fixMode: "guided",
        rationale: "键名疑似凭证，但当前证据不能区分明文值和安全引用。",
        nextSteps: ["人工确认值类型；若为明文则改用安全注入并轮换凭证。"],
        verification: ["确认配置不再保存明文值；引用型配置可记录为误报。"],
        acceptWhen: "值只是安全引用/占位符，或凭证是经批准的短期例外。",
        family: "mcp.server", evidenceKeys: ["server"],
    }),
    GEMINI_SHELL_NO_SANDBOX: action({
        disposition: "fix", priority: "P1", confidence: "high", fixMode: "guided",
        rationale: "shell 工具直接在宿主机执行，提示注入或误操作会造成真实破坏。",
        nextSteps: ["启用受支持 sandbox，或禁用 run_shell_command。"],
        verification: verifyRescan,
        acceptWhen: "运行环境本身是无敏感数据和凭证的隔离沙箱。",
        family: "permission.execution", evidenceKeys: ["shellToolAllowed", "sandbox"],
    }),
    GEMINI_AUTH_MODE: action({
        disposition: "observe", priority: "P3", confidence: "high", fixMode: "none",
        rationale: "鉴权模式是资产信息，本身不是漏洞。",
        nextSteps: ["确认该鉴权方式符合团队凭证管理规范。"],
        verification: ["将鉴权模式与明文凭证发现对照检查。"],
        acceptWhen: "鉴权来源和凭证存放方式符合团队规范。",
        family: "provider.auth-mode", evidenceKeys: ["authType"],
    }),
    GEMINI_PARSE_FAILED: action({
        disposition: "review", priority: "P1", confidence: "high", fixMode: "guided",
        rationale: "解析失败会造成 Gemini 配置扫描盲区。",
        nextSteps: ["校验 settings.json 格式和读取权限。"],
        verification: verifyParse,
        family: "coverage.parse", evidenceKeys: ["path"],
    }),
    OPENCLAW_CHANNEL_PLAINTEXT_SECRET: action({
        disposition: "fix", priority: "P0", confidence: "high", fixMode: "guided",
        rationale: "渠道 appSecret 随配置文件泄露。",
        nextSteps: ["改用环境变量/密钥管理器注入，并轮换旧 secret。"],
        verification: verifyRescan,
        family: "secret.plaintext", evidenceKeys: ["channel", "field"],
    }),
    OPENCLAW_CHANNEL_PLAINTEXT_TOKEN: action({
        disposition: "fix", priority: "P0", confidence: "high", fixMode: "guided",
        rationale: "渠道 token 随配置文件泄露。",
        nextSteps: ["改用环境变量/密钥管理器注入，并轮换旧 token。"],
        verification: verifyRescan,
        family: "secret.plaintext", evidenceKeys: ["channel", "field"],
    }),
    OPENCLAW_GATEWAY_PLAINTEXT_TOKEN: action({
        disposition: "fix", priority: "P0", confidence: "high", fixMode: "guided",
        rationale: "网关 token 可授予完整 API 权限，明文落盘影响直接。",
        nextSteps: ["改用环境变量/密钥管理器注入，并轮换旧 token。"],
        verification: verifyRescan,
        family: "secret.plaintext", evidenceKeys: ["field"],
    }),
    OPENCLAW_GATEWAY_EXPOSED_BIND: action({
        disposition: "fix", priority: "P0", confidence: "high", fixMode: "baseline",
        rationale: "网关监听非 loopback 地址会扩大到同网段或公网访问面。",
        nextSteps: ["使用 baseline 收敛到 127.0.0.1；远程访问改走受控 VPN/Tailnet。"],
        verification: verifyRescan,
        acceptWhen: "远程访问是明确需求，且已有强鉴权、网络 ACL 和监控。",
        baselineProfiles: { safe: "resolve", balanced: "resolve" },
        family: "openclaw.gateway-exposure", evidenceKeys: ["bind", "port"],
    }),
    OPENCLAW_TAILSCALE_EXPOSURE: action({
        disposition: "fix", priority: "P0", confidence: "high", fixMode: "baseline",
        rationale: "Funnel/public/expose 会把网关发布到更广网络或公网。",
        nextSteps: ["使用 baseline 关闭公开暴露，仅保留受控 Tailnet 访问。"],
        verification: verifyRescan,
        acceptWhen: "公开服务是明确需求，且已有强鉴权、最小权限和持续监控。",
        baselineProfiles: { safe: "resolve", balanced: "resolve" },
        family: "openclaw.gateway-exposure", evidenceKeys: ["mode"],
    }),
    OPENCLAW_AGENT_WORKSPACE_OVERLAP: action({
        disposition: "review", priority: "P2", confidence: "high", fixMode: "manual",
        rationale: "多个 Agent 共享 workspace 可能导致上下文、记忆和权限互相影响。",
        nextSteps: ["确认共享是否必要；否则为每个 Agent 分配独立 workspace。"],
        verification: verifyRescan,
        acceptWhen: "共享是明确设计，参与 Agent 的信任边界和数据范围一致。",
        family: "workspace.overlap", evidenceKeys: ["workspace"],
    }),
    OPENCLAW_UNKNOWN_PLUGIN_SOURCE: action({
        disposition: "review", priority: "P1", confidence: "medium", fixMode: "manual",
        rationale: "file/http 等非标准插件来源绕过常规包发布和版本验证流程。",
        nextSteps: ["核对代码来源、版本/hash 和维护者；优先改用可信 npm 包。"],
        verification: verifyRescan,
        acceptWhen: "插件代码经审核、来源受控、版本或哈希固定。",
        family: "supply-chain.plugin", evidenceKeys: ["plugin", "source"],
    }),
    OPENCLAW_SERVICE_ENV_PRESENT: action({
        disposition: "observe", priority: "P3", confidence: "medium", fixMode: "none",
        rationale: "只确认 env 文件存在，未读取内容；它是凭证存储清单，不是默认漏洞。",
        nextSteps: ["确认文件归属当前用户、权限为 600，且不进入同步或仓库。"],
        verification: ["检查 service-env 文件及父目录的所有者和权限。"],
        acceptWhen: "文件权限收紧、内容确有必要且不进入仓库/不安全备份。",
        family: "secret.file", evidenceKeys: ["path"],
    }),
    OPENCLAW_PARSE_FAIL: action({
        disposition: "review", priority: "P1", confidence: "high", fixMode: "guided",
        rationale: "解析失败会使网关暴露、渠道凭证和插件风险不可见。",
        nextSteps: ["校验 openclaw.json 格式和读取权限。"],
        verification: verifyParse,
        family: "coverage.parse", evidenceKeys: ["path"],
    }),
    XAGENT_SHARED_PROXY: action({
        disposition: "review", priority: "P1", confidence: "high", fixMode: "manual",
        rationale: "多个 Agent 汇聚到同一本地代理，形成可观察提示词和凭证的集中单点。",
        nextSteps: ["核对代理进程、所有者、更新来源、规则和全部真实上游。"],
        verification: ["确认每个关联 Agent 的链路符合预期，并评估是否需要隔离出口。"],
        acceptWhen: "代理由组织控制、上游均可信且集中风险已有监控和补偿控制。",
        family: "correlation.shared-proxy", evidenceKeys: ["proxy"],
    }),
    XAGENT_SHARED_ENDPOINT: action({
        disposition: "review", priority: "P1", confidence: "medium", fixMode: "manual",
        rationale: "多个 Agent 向同一非官方端点发送上下文，会扩大集中泄露影响。",
        nextSteps: ["核实端点归属、数据政策和各 Agent 是否确需共用。"],
        verification: ["逐个确认关联 Agent 的 Provider 配置，移除不必要连接后复扫。"],
        acceptWhen: "端点经批准、数据处理政策明确且共用是有意设计。",
        family: "correlation.shared-endpoint", evidenceKeys: ["endpoint"],
    }),
    PROJECT_SENSITIVE_FILE: action({
        disposition: "review", priority: "P1", confidence: "medium", fixMode: "manual",
        rationale: "敏感路径位于 Agent 工作目录内，可能被读取或上传；规则未读取文件内容。",
        nextSteps: ["确认文件是否必须留在项目中；否则移出，或加入 Agent deny/ignore。"],
        verification: verifyRescan,
        acceptWhen: "文件是安全样例/占位符，或已有可靠 deny、权限和数据边界控制。",
        family: "project.sensitive-file", evidenceKeys: ["path", "kind"],
    }),
    PROJECT_SENSITIVE_SCAN_TRUNCATED: action({
        disposition: "review", priority: "P1", confidence: "high", fixMode: "none",
        rationale: "结果达到上限，当前报告不能代表项目内全部敏感文件。",
        nextSteps: ["先处理已显示结果，再缩小扫描目录或使用专用工具完成全量检查。"],
        verification: ["确认后续扫描不再截断，或已由其他全量扫描覆盖。"],
        acceptWhen: "已有等价的全量敏感文件扫描覆盖。",
        family: "coverage.truncated", evidenceKeys: ["maxFindings"],
    }),
};
/** 为扫描汇总阶段提供安全的字符串规则 ID 查询。 */
export function getRuleAction(id) {
    return Object.prototype.hasOwnProperty.call(ACTION_MATRIX, id)
        ? ACTION_MATRIX[id]
        : undefined;
}
/** 兼容 action enrichment 层使用的语义化命名。 */
export const getFindingAction = getRuleAction;
//# sourceMappingURL=action-matrix.js.map